apiVersion: batch/v1
kind: Job
metadata:
  name: pg-restore-job-supplier-edc
  namespace: product-puris
spec:
  backoffLimit: 3
  template:
    spec:
      restartPolicy: OnFailure
      containers:
      - name: restore
        image: postgres:18
        command:
        - /bin/sh
        - -c
        - |
          echo "Starting restore..."
          pg_restore -h supplier-edc-postgresql -U postgres -d edc -c --if-exists -1 /migration/db.dump
          echo "Restore complete!"
        env:
        - name: PGPASSWORD
          valueFrom:
            secretKeyRef:
              name: supplier-edc-postgresql
              key: postgres-password
        volumeMounts:
        - name: storage
          mountPath: /migration
      volumes:
      - name: storage
        persistentVolumeClaim:
          claimName: migration-pvc-supplier-edc