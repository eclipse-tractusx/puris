meta {
  name: Get Reported Data Exchange Request
  type: http
  seq: 2
}

get {
  url: {{CUSTOMER_PURIS_BACKEND}}/catena/data-exchange-request/reported
  body: none
  auth: none
}

headers {
  X-Api-Key: {{CUSTOMER_PURIS_BACKEND_API_KEY}}
}

script:pre-request {
  console.log("Waiting 5 seconds");
  await (new Promise(resolve => setTimeout(resolve, 5000)));
}

tests {
  test("response is ok", function() {
      expect(res.getStatus()).to.equal(200);
  })

  test("Verify response contains one reported data exchange request object", function () {
      expect(res.getBody()).to.be.an("array").with.lengthOf(1);
  });
  
}
